# UI Standards

- Use **shadcn/ui** components exclusively for all UI elements
- Do not create custom components — always use an existing shadcn/ui component first
- If a suitable component is not installed, check the [shadcn/ui registry](https://ui.shadcn.com) for an appropriate one and install it via the CLI before proceeding
